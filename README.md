# C++ Projects

This repository contains various C++ projects.

## Conan

To set up your Conan profile, run the following command:

```bash
conan profile detect --name=default
```

This command is used to define a new default profile with the current system settings.